prompt:
in esp idf
you can use rtc memory
you can use custom partition
the goal is to call a function(theDo) defined in the main program
from a hook in the bootloader
so you need to know the adress of the function.
keep in mind that in every build the address might change.
you can use linker script


